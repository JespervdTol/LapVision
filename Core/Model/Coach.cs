using System.Collections.Generic;

namespace Core.Model
{
    public class Coach : Person
    {
        // Example: Drivers managed by this coach
        //public ICollection<Driver> Drivers { get; set; } = new List<Driver>();

        //public void SetBenchmarkLap(Lap lap) { }
        //public Report DownloadReport(Driver driver) => new Report();
        //public Driver SelectDriver(Driver driver) => driver;
    }
}